<input matInput type="number" [(ngModel)]="item.parentMenuId"
    (focus)="beginParentEdit(item)" (change)="commitParentEdit(item)"


 <ng-container matColumnDef="sortOrder">
     <th mat-header-cell *matHeaderCellDef scope="col">Position in group</th>
     <td mat-cell *matCellDef="let item">
         <span class="admin-position">{{ positionLabel(item) }}</span>
     </td>
 </ng-container>

.admin-position {
  font-variant-numeric: tabular-nums;
  font-weight: 600;
}

 this.sharedService.sorting(subMenuList, "sortOrder");

 order: element.sortOrder ?? element.order,